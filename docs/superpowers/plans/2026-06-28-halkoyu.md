# Halkoyu Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Türkçe halk oylaması sitesi — ziyaretçiler e-posta OTP ile 2 seçenekli anketlere oy kullanır, sonuçlar herkese açıktır.

**Architecture:** SvelteKit full-stack app; server routes handle OTP and vote logic using the Supabase service key (never exposed to client). The client uses only the anon key for reading public data. E-mail addresses are never stored — only `SHA-256(email + anket_id)` hash.

**Tech Stack:** SvelteKit 2, Supabase (PostgreSQL + Auth OTP), Tailwind CSS 3, TypeScript, Vitest

## Global Constraints

- Dil: Türkçe — tüm UI metinleri Türkçe olacak
- E-posta saklanmaz — her zaman `SHA-256(email + anket_id)` hash kullanılır
- Oy kaydı yalnızca server-side endpoint'ten yapılır (`SUPABASE_SERVICE_KEY`)
- Anket formatı yalnızca 2 seçenek: A ve B
- Node.js >= 18 (Web Crypto API built-in)
- Tailwind CSS utility-first, özel CSS yazılmaz
- Her task sonunda `git commit` yapılır

---

## Dosya Haritası

```
halkoyu/
├── src/
│   ├── app.html                              # HTML shell
│   ├── app.css                               # Tailwind direktifleri
│   ├── lib/
│   │   ├── supabase.ts                       # Server-side Supabase client (service key)
│   │   ├── supabase-browser.ts               # Client-side Supabase client (anon key)
│   │   ├── hash.ts                           # SHA-256(email + anket_id) yardımcı
│   │   └── components/
│   │       ├── AnketKarti.svelte             # Ana sayfadaki anket kartı
│   │       ├── SonucBar.svelte               # Sonuç bar chart bileşeni
│   │       └── OyFormu.svelte                # E-posta + OTP inline formu
│   └── routes/
│       ├── +layout.svelte                    # Global layout
│       ├── +page.svelte                      # Ana sayfa
│       ├── +page.server.ts                   # Ana sayfa load (anketler listesi)
│       ├── anket/[slug]/
│       │   ├── +page.svelte                  # Anket detay sayfası
│       │   └── +page.server.ts               # Anket + oy sayıları load
│       └── api/
│           ├── otp/+server.ts                # POST /api/otp — OTP gönder
│           └── oy/+server.ts                 # POST /api/oy  — OTP doğrula + oy kaydet
├── supabase/
│   └── migrations/
│       └── 001_initial.sql                   # Tablo şemaları
├── src/tests/
│   ├── hash.test.ts                          # hash.ts unit testi
│   ├── api-otp.test.ts                       # /api/otp endpoint testi
│   └── api-oy.test.ts                        # /api/oy endpoint testi
├── .env.example
├── svelte.config.js
├── vite.config.ts
├── tailwind.config.ts
└── package.json
```

---

### Task 1: Proje Scaffolding

**Files:**
- Create: `package.json`, `svelte.config.js`, `vite.config.ts`, `tailwind.config.ts`, `src/app.html`, `src/app.css`, `src/routes/+layout.svelte`, `.env.example`

**Interfaces:**
- Produces: Çalışan boş SvelteKit uygulaması, `npm run dev` ile ayağa kalkar

- [ ] **Step 1: SvelteKit projesi oluştur**

```bash
cd C:\Users\merta\OneDrive\Documents\DEV\GOLANG
npx sv create halkoyu
```

Sorularda seç:
- Template: **SvelteKit minimal**
- Type checking: **TypeScript**
- Add-ons: **Tailwind CSS**, **Vitest**

- [ ] **Step 2: Supabase JS kütüphanesini ekle**

```bash
cd halkoyu
npm install @supabase/supabase-js
```

- [ ] **Step 3: `.env.example` dosyasını oluştur**

```
# .env.example
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-role-key
```

- [ ] **Step 4: `src/app.css` içine Tailwind direktifleri yaz**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 5: `src/routes/+layout.svelte` oluştur**

```svelte
<script>
  import '../app.css';
</script>

<main class="min-h-screen bg-gray-50 text-gray-900">
  <header class="bg-white border-b border-gray-200 px-4 py-3">
    <a href="/" class="text-xl font-bold text-blue-600">Halk Oylaması</a>
  </header>
  <slot />
</main>
```

- [ ] **Step 6: Uygulamanın ayağa kalktığını doğrula**

```bash
npm run dev
```

Tarayıcıda `http://localhost:5173` açılmalı, boş sayfa görünmeli.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: SvelteKit + Tailwind + Supabase scaffolding"
```

---

### Task 2: Supabase Veritabanı Şeması

**Files:**
- Create: `supabase/migrations/001_initial.sql`

**Interfaces:**
- Produces: `anketler`, `oylar`, `otp_oturumlari` tabloları Supabase'de hazır

- [ ] **Step 1: Migration dosyasını oluştur**

`supabase/migrations/001_initial.sql`:

```sql
-- Anketler tablosu
CREATE TABLE anketler (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  slug        text UNIQUE NOT NULL,
  soru        text NOT NULL,
  secenek_a   text NOT NULL,
  secenek_b   text NOT NULL,
  aktif       boolean NOT NULL DEFAULT true,
  created_at  timestamptz NOT NULL DEFAULT now()
);

-- Oylar tablosu (e-posta saklanmaz, sadece hash)
CREATE TABLE oylar (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  anket_id    uuid NOT NULL REFERENCES anketler(id),
  email_hash  text NOT NULL,
  secim       char(1) NOT NULL CHECK (secim IN ('A', 'B')),
  created_at  timestamptz NOT NULL DEFAULT now(),
  UNIQUE(anket_id, email_hash)
);

-- OTP oturumları (10 dakika geçerli)
CREATE TABLE otp_oturumlari (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  email_hash  text NOT NULL,
  anket_id    uuid NOT NULL REFERENCES anketler(id),
  dogrulandi  boolean NOT NULL DEFAULT false,
  expires_at  timestamptz NOT NULL,
  created_at  timestamptz NOT NULL DEFAULT now()
);

-- Anketler herkese okunabilir (RLS)
ALTER TABLE anketler ENABLE ROW LEVEL SECURITY;
CREATE POLICY "anketler_public_read" ON anketler
  FOR SELECT TO anon USING (true);

-- Oylar sayısı herkese okunabilir, yazma yasak (server key ile yapılır)
ALTER TABLE oylar ENABLE ROW LEVEL SECURITY;
CREATE POLICY "oylar_public_read" ON oylar
  FOR SELECT TO anon USING (true);

-- otp_oturumlari sadece service key ile erişilebilir
ALTER TABLE otp_oturumlari ENABLE ROW LEVEL SECURITY;
```

- [ ] **Step 2: Supabase'e migration'ı uygula**

[Supabase Dashboard](https://supabase.com/dashboard) → projen → SQL Editor → yukarıdaki SQL'i yapıştır ve çalıştır.

- [ ] **Step 3: Test verisi ekle**

SQL Editor'de:

```sql
INSERT INTO anketler (slug, soru, secenek_a, secenek_b) VALUES
  ('turkiye-ab', 'Türkiye AB''ye üye olmalı mı?', 'Evet', 'Hayır'),
  ('uzaktan-calisma', 'Uzaktan çalışma kalıcı olmalı mı?', 'Evet', 'Hayır');
```

- [ ] **Step 4: Commit**

```bash
git add supabase/
git commit -m "feat: veritabanı şeması ve RLS politikaları"
```

---

### Task 3: Supabase Client + Hash Yardımcısı

**Files:**
- Create: `src/lib/supabase.ts`, `src/lib/supabase-browser.ts`, `src/lib/hash.ts`
- Create: `src/tests/hash.test.ts`

**Interfaces:**
- Produces:
  - `supabaseServer` — `SupabaseClient` (service key, yalnızca server routes'ta kullanılır)
  - `supabaseBrowser` — `SupabaseClient` (anon key, client-side)
  - `emailHash(email: string, anketId: string): Promise<string>` — hex string döner

- [ ] **Step 1: Hash testi yaz**

`src/tests/hash.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { emailHash } from '$lib/hash';

describe('emailHash', () => {
  it('aynı girdi için aynı hash üretir', async () => {
    const h1 = await emailHash('test@example.com', 'anket-123');
    const h2 = await emailHash('test@example.com', 'anket-123');
    expect(h1).toBe(h2);
  });

  it('farklı e-posta için farklı hash üretir', async () => {
    const h1 = await emailHash('a@example.com', 'anket-123');
    const h2 = await emailHash('b@example.com', 'anket-123');
    expect(h1).not.toBe(h2);
  });

  it('farklı anket için farklı hash üretir', async () => {
    const h1 = await emailHash('test@example.com', 'anket-1');
    const h2 = await emailHash('test@example.com', 'anket-2');
    expect(h1).not.toBe(h2);
  });

  it('64 karakterlik hex string döner', async () => {
    const h = await emailHash('test@example.com', 'anket-123');
    expect(h).toMatch(/^[a-f0-9]{64}$/);
  });
});
```

- [ ] **Step 2: Testi çalıştır, başarısız olduğunu doğrula**

```bash
npm run test -- src/tests/hash.test.ts
```

Beklenen: `Cannot find module '$lib/hash'`

- [ ] **Step 3: `src/lib/hash.ts` yaz**

```typescript
export async function emailHash(email: string, anketId: string): Promise<string> {
  const input = email.toLowerCase().trim() + anketId;
  const encoded = new TextEncoder().encode(input);
  const hashBuffer = await crypto.subtle.digest('SHA-256', encoded);
  return Array.from(new Uint8Array(hashBuffer))
    .map((b) => b.toString(16).padStart(2, '0'))
    .join('');
}
```

- [ ] **Step 4: Testi çalıştır, geçtiğini doğrula**

```bash
npm run test -- src/tests/hash.test.ts
```

Beklenen: 4 test PASS

- [ ] **Step 5: `src/lib/supabase.ts` (server-side, service key) yaz**

```typescript
import { createClient } from '@supabase/supabase-js';
import { PUBLIC_SUPABASE_URL } from '$env/static/public';
import { SUPABASE_SERVICE_KEY } from '$env/static/private';

export const supabaseServer = createClient(PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_KEY);
```

- [ ] **Step 6: `src/lib/supabase-browser.ts` (client-side, anon key) yaz**

```typescript
import { createClient } from '@supabase/supabase-js';
import { PUBLIC_SUPABASE_URL, PUBLIC_SUPABASE_ANON_KEY } from '$env/static/public';

export const supabaseBrowser = createClient(PUBLIC_SUPABASE_URL, PUBLIC_SUPABASE_ANON_KEY);
```

- [ ] **Step 7: `.env` dosyasını oluştur (Supabase dashboard'dan değerleri al)**

```
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_KEY=eyJ...
```

- [ ] **Step 8: Commit**

```bash
git add src/lib/ src/tests/hash.test.ts .env.example
git commit -m "feat: supabase client ve email hash yardımcısı"
```

---

### Task 4: OTP API Endpoint'i

**Files:**
- Create: `src/routes/api/otp/+server.ts`
- Create: `src/tests/api-otp.test.ts`

**Interfaces:**
- Consumes: `supabaseServer` (`src/lib/supabase.ts`), `emailHash` (`src/lib/hash.ts`)
- Produces:
  - `POST /api/otp` body: `{ email: string, anketId: string }` → `{ ok: true }` veya `{ error: string }`

- [ ] **Step 1: OTP endpoint testini yaz**

`src/tests/api-otp.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

// Supabase mock
const mockSignInWithOtp = vi.fn();
vi.mock('$lib/supabase', () => ({
  supabaseServer: {
    auth: { signInWithOtp: mockSignInWithOtp },
    from: vi.fn(() => ({
      select: vi.fn(() => ({ eq: vi.fn(() => ({ single: vi.fn(() => ({ data: { id: 'anket-123', aktif: true }, error: null })) })) }))
    }))
  }
}));

vi.mock('$env/static/public', () => ({
  PUBLIC_SUPABASE_URL: 'https://test.supabase.co',
  PUBLIC_SUPABASE_ANON_KEY: 'test-anon-key'
}));

vi.mock('$env/static/private', () => ({
  SUPABASE_SERVICE_KEY: 'test-service-key'
}));

import { POST } from '../routes/api/otp/+server';

describe('POST /api/otp', () => {
  beforeEach(() => { mockSignInWithOtp.mockReset(); });

  it('geçerli istek için OTP gönderir', async () => {
    mockSignInWithOtp.mockResolvedValue({ error: null });
    const req = new Request('http://localhost/api/otp', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email: 'test@example.com', anketId: 'anket-123' })
    });
    const res = await POST({ request: req } as any);
    const json = await res.json();
    expect(res.status).toBe(200);
    expect(json.ok).toBe(true);
  });

  it('eksik alan için 400 döner', async () => {
    const req = new Request('http://localhost/api/otp', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email: '' })
    });
    const res = await POST({ request: req } as any);
    expect(res.status).toBe(400);
  });
});
```

- [ ] **Step 2: Testi çalıştır, başarısız olduğunu doğrula**

```bash
npm run test -- src/tests/api-otp.test.ts
```

Beklenen: `Cannot find module '../routes/api/otp/+server'`

- [ ] **Step 3: `src/routes/api/otp/+server.ts` yaz**

```typescript
import { json } from '@sveltejs/kit';
import type { RequestHandler } from './$types';
import { supabaseServer } from '$lib/supabase';

export const POST: RequestHandler = async ({ request }) => {
  const body = await request.json().catch(() => null);
  const email: string = body?.email?.trim() ?? '';
  const anketId: string = body?.anketId?.trim() ?? '';

  if (!email || !anketId) {
    return json({ error: 'Geçersiz istek' }, { status: 400 });
  }

  // Anketin var olup olmadığını ve aktif olduğunu kontrol et
  const { data: anket, error: anketError } = await supabaseServer
    .from('anketler')
    .select('id, aktif')
    .eq('id', anketId)
    .single();

  if (anketError || !anket) {
    return json({ error: 'Anket bulunamadı' }, { status: 404 });
  }

  if (!anket.aktif) {
    return json({ error: 'Bu anket kapalı' }, { status: 403 });
  }

  const { error } = await supabaseServer.auth.signInWithOtp({
    email,
    options: { shouldCreateUser: false }
  });

  if (error) {
    return json({ error: 'Kod gönderilemedi, lütfen tekrar deneyin' }, { status: 500 });
  }

  return json({ ok: true });
};
```

- [ ] **Step 4: Testi çalıştır, geçtiğini doğrula**

```bash
npm run test -- src/tests/api-otp.test.ts
```

Beklenen: 2 test PASS

- [ ] **Step 5: Commit**

```bash
git add src/routes/api/otp/ src/tests/api-otp.test.ts
git commit -m "feat: OTP gönderme endpoint'i"
```

---

### Task 5: Oy Kaydetme API Endpoint'i

**Files:**
- Create: `src/routes/api/oy/+server.ts`
- Create: `src/tests/api-oy.test.ts`

**Interfaces:**
- Consumes: `supabaseServer`, `emailHash`
- Produces:
  - `POST /api/oy` body: `{ email: string, anketId: string, kod: string, secim: 'A' | 'B' }` → `{ ok: true, oylar: { a: number, b: number } }` veya `{ error: string }`

- [ ] **Step 1: Oy endpoint testini yaz**

`src/tests/api-oy.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

const mockVerifyOtp = vi.fn();
const mockFrom = vi.fn();

vi.mock('$lib/supabase', () => ({
  supabaseServer: {
    auth: { verifyOtp: mockVerifyOtp },
    from: mockFrom
  }
}));

vi.mock('$env/static/public', () => ({
  PUBLIC_SUPABASE_URL: 'https://test.supabase.co',
  PUBLIC_SUPABASE_ANON_KEY: 'test-anon-key'
}));

vi.mock('$env/static/private', () => ({
  SUPABASE_SERVICE_KEY: 'test-service-key'
}));

import { POST } from '../routes/api/oy/+server';

function makeChain(overrides: Record<string, unknown> = {}) {
  const chain = {
    select: vi.fn().mockReturnThis(),
    eq: vi.fn().mockReturnThis(),
    single: vi.fn().mockResolvedValue({ data: { id: 'anket-uuid', aktif: true }, error: null }),
    insert: vi.fn().mockResolvedValue({ error: null }),
    ...overrides
  };
  return chain;
}

describe('POST /api/oy', () => {
  beforeEach(() => {
    mockVerifyOtp.mockReset();
    mockFrom.mockReset();
  });

  it('geçerli istek için oy kaydeder ve sonuçları döner', async () => {
    mockVerifyOtp.mockResolvedValue({ error: null });

    // anketler.select, oylar.insert, oylar count A, oylar count B
    let callCount = 0;
    mockFrom.mockImplementation((table: string) => {
      callCount++;
      if (table === 'anketler') return makeChain();
      if (table === 'oylar' && callCount === 2) return { insert: vi.fn().mockResolvedValue({ error: null }) };
      // count queries
      return {
        select: vi.fn().mockReturnThis(),
        eq: vi.fn().mockReturnThis(),
        then: vi.fn().mockResolvedValue({ count: 5, error: null })
      };
    });

    const req = new Request('http://localhost/api/oy', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email: 'test@example.com', anketId: 'anket-uuid', kod: '123456', secim: 'A' })
    });
    const res = await POST({ request: req } as any);
    expect(res.status).toBe(200);
  });

  it('geçersiz secim için 400 döner', async () => {
    const req = new Request('http://localhost/api/oy', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email: 'test@example.com', anketId: 'anket-uuid', kod: '123456', secim: 'C' })
    });
    const res = await POST({ request: req } as any);
    expect(res.status).toBe(400);
  });

  it('yanlış OTP için 401 döner', async () => {
    mockVerifyOtp.mockResolvedValue({ error: { message: 'Invalid OTP' } });
    mockFrom.mockImplementation(() => makeChain());

    const req = new Request('http://localhost/api/oy', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email: 'test@example.com', anketId: 'anket-uuid', kod: '000000', secim: 'A' })
    });
    const res = await POST({ request: req } as any);
    expect(res.status).toBe(401);
  });
});
```

- [ ] **Step 2: Testi çalıştır, başarısız olduğunu doğrula**

```bash
npm run test -- src/tests/api-oy.test.ts
```

Beklenen: `Cannot find module '../routes/api/oy/+server'`

- [ ] **Step 3: `src/routes/api/oy/+server.ts` yaz**

```typescript
import { json } from '@sveltejs/kit';
import type { RequestHandler } from './$types';
import { supabaseServer } from '$lib/supabase';
import { emailHash } from '$lib/hash';

export const POST: RequestHandler = async ({ request }) => {
  const body = await request.json().catch(() => null);
  const email: string = body?.email?.trim() ?? '';
  const anketId: string = body?.anketId?.trim() ?? '';
  const kod: string = body?.kod?.trim() ?? '';
  const secim: string = body?.secim ?? '';

  if (!email || !anketId || !kod || !['A', 'B'].includes(secim)) {
    return json({ error: 'Geçersiz istek' }, { status: 400 });
  }

  // Anketin aktif olduğunu doğrula
  const { data: anket, error: anketError } = await supabaseServer
    .from('anketler')
    .select('id, aktif')
    .eq('id', anketId)
    .single();

  if (anketError || !anket) {
    return json({ error: 'Anket bulunamadı' }, { status: 404 });
  }

  if (!anket.aktif) {
    return json({ error: 'Bu anket kapalı' }, { status: 403 });
  }

  // OTP'yi doğrula
  const { error: otpError } = await supabaseServer.auth.verifyOtp({
    email,
    token: kod,
    type: 'email'
  });

  if (otpError) {
    return json({ error: 'Kod geçersiz veya süresi dolmuş' }, { status: 401 });
  }

  const hash = await emailHash(email, anketId);

  // Oyu kaydet (UNIQUE constraint duplicate'ı engeller)
  const { error: insertError } = await supabaseServer
    .from('oylar')
    .insert({ anket_id: anketId, email_hash: hash, secim });

  if (insertError) {
    if (insertError.code === '23505') {
      return json({ error: 'Bu anket için zaten oy kullandınız' }, { status: 409 });
    }
    return json({ error: 'Oy kaydedilemedi, tekrar deneyin' }, { status: 500 });
  }

  // Güncel sonuçları döndür
  const [{ count: oyA }, { count: oyB }] = await Promise.all([
    supabaseServer.from('oylar').select('*', { count: 'exact', head: true }).eq('anket_id', anketId).eq('secim', 'A'),
    supabaseServer.from('oylar').select('*', { count: 'exact', head: true }).eq('anket_id', anketId).eq('secim', 'B')
  ]);

  return json({ ok: true, oylar: { a: oyA ?? 0, b: oyB ?? 0 } });
};
```

- [ ] **Step 4: Testi çalıştır, geçtiğini doğrula**

```bash
npm run test -- src/tests/api-oy.test.ts
```

Beklenen: 3 test PASS

- [ ] **Step 5: Commit**

```bash
git add src/routes/api/oy/ src/tests/api-oy.test.ts
git commit -m "feat: oy kaydetme endpoint'i (OTP doğrulama + duplicate koruması)"
```

---

### Task 6: UI Bileşenleri

**Files:**
- Create: `src/lib/components/SonucBar.svelte`
- Create: `src/lib/components/OyFormu.svelte`
- Create: `src/lib/components/AnketKarti.svelte`

**Interfaces:**
- Consumes: Hiçbir dış bağımlılık (pure Svelte components)
- Produces:
  - `<SonucBar secenek="Evet" oy={120} toplamOy={200} />` — tek seçeneğin bar'ı
  - `<OyFormu anketId="uuid" on:oyKaydedildi />` — oy formu, event: `CustomEvent<{ oylar: { a: number, b: number } }>`
  - `<AnketKarti anket={anket} />` — liste kartı

- [ ] **Step 1: `src/lib/components/SonucBar.svelte` yaz**

```svelte
<script lang="ts">
  export let secenek: string;
  export let oy: number;
  export let toplamOy: number;

  $: yuzde = toplamOy === 0 ? 0 : Math.round((oy / toplamOy) * 100);
</script>

<div class="mb-2">
  <div class="flex justify-between text-sm mb-1">
    <span class="font-medium">{secenek}</span>
    <span class="text-gray-500">{yuzde}% ({oy} oy)</span>
  </div>
  <div class="w-full bg-gray-200 rounded-full h-4">
    <div
      class="bg-blue-500 h-4 rounded-full transition-all duration-300"
      style="width: {yuzde}%"
    ></div>
  </div>
</div>
```

- [ ] **Step 2: `src/lib/components/OyFormu.svelte` yaz**

```svelte
<script lang="ts">
  import { createEventDispatcher } from 'svelte';

  export let anketId: string;
  export let secenekA: string;
  export let secenekB: string;

  const dispatch = createEventDispatcher<{ oyKaydedildi: { oylar: { a: number; b: number } } }>();

  let adim: 'secim' | 'eposta' | 'kod' | 'tamam' = 'secim';
  let secilenSecim: 'A' | 'B' | null = null;
  let eposta = '';
  let kod = '';
  let hata = '';
  let yukleniyor = false;

  async function otpGonder() {
    hata = '';
    yukleniyor = true;
    const res = await fetch('/api/otp', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email: eposta, anketId })
    });
    const data = await res.json();
    yukleniyor = false;
    if (!res.ok) { hata = data.error; return; }
    adim = 'kod';
  }

  async function oyKullan() {
    hata = '';
    yukleniyor = true;
    const res = await fetch('/api/oy', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email: eposta, anketId, kod, secim: secilenSecim })
    });
    const data = await res.json();
    yukleniyor = false;
    if (!res.ok) { hata = data.error; return; }
    adim = 'tamam';
    dispatch('oyKaydedildi', { oylar: data.oylar });
  }
</script>

{#if adim === 'secim'}
  <div class="space-y-2">
    <p class="text-sm text-gray-600 mb-3">Oyunuzu kullanmak için bir seçenek seçin:</p>
    <button
      class="w-full py-2 px-4 rounded border-2 {secilenSecim === 'A' ? 'border-blue-500 bg-blue-50' : 'border-gray-300'}"
      on:click={() => { secilenSecim = 'A'; adim = 'eposta'; }}
    >{secenekA}</button>
    <button
      class="w-full py-2 px-4 rounded border-2 {secilenSecim === 'B' ? 'border-blue-500 bg-blue-50' : 'border-gray-300'}"
      on:click={() => { secilenSecim = 'B'; adim = 'eposta'; }}
    >{secenekB}</button>
  </div>

{:else if adim === 'eposta'}
  <div class="space-y-3">
    <p class="text-sm text-gray-600">E-posta adresinize doğrulama kodu gönderilecek:</p>
    <input
      type="email"
      bind:value={eposta}
      placeholder="ornek@eposta.com"
      class="w-full border border-gray-300 rounded px-3 py-2 text-sm"
    />
    {#if hata}<p class="text-red-500 text-sm">{hata}</p>{/if}
    <button
      on:click={otpGonder}
      disabled={yukleniyor || !eposta}
      class="w-full bg-blue-600 text-white py-2 rounded disabled:opacity-50"
    >{yukleniyor ? 'Gönderiliyor...' : 'Kod Gönder'}</button>
  </div>

{:else if adim === 'kod'}
  <div class="space-y-3">
    <p class="text-sm text-gray-600">E-postanıza gelen 6 haneli kodu girin:</p>
    <input
      type="text"
      bind:value={kod}
      placeholder="123456"
      maxlength="6"
      class="w-full border border-gray-300 rounded px-3 py-2 text-sm tracking-widest text-center text-lg"
    />
    {#if hata}<p class="text-red-500 text-sm">{hata}</p>{/if}
    <button
      on:click={oyKullan}
      disabled={yukleniyor || kod.length !== 6}
      class="w-full bg-blue-600 text-white py-2 rounded disabled:opacity-50"
    >{yukleniyor ? 'Doğrulanıyor...' : 'Onayla ve Oy Ver'}</button>
  </div>

{:else}
  <p class="text-green-600 font-medium text-center py-4">Oyunuz kaydedildi!</p>
{/if}
```

- [ ] **Step 3: `src/lib/components/AnketKarti.svelte` yaz**

```svelte
<script lang="ts">
  export let anket: {
    id: string;
    slug: string;
    soru: string;
    aktif: boolean;
    oy_a: number;
    oy_b: number;
  };

  $: toplam = anket.oy_a + anket.oy_b;
</script>

<a href="/anket/{anket.slug}" class="block bg-white rounded-lg border border-gray-200 p-4 hover:border-blue-300 transition-colors">
  <div class="flex justify-between items-start mb-2">
    <h2 class="font-semibold text-gray-900">{anket.soru}</h2>
    <span class="text-xs px-2 py-1 rounded-full ml-2 shrink-0 {anket.aktif ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'}">
      {anket.aktif ? 'Aktif' : 'Kapalı'}
    </span>
  </div>
  <p class="text-sm text-gray-500">{toplam} oy</p>
</a>
```

- [ ] **Step 4: Commit**

```bash
git add src/lib/components/
git commit -m "feat: UI bileşenleri (SonucBar, OyFormu, AnketKarti)"
```

---

### Task 7: Ana Sayfa

**Files:**
- Create: `src/routes/+page.server.ts`
- Modify: `src/routes/+page.svelte`

**Interfaces:**
- Consumes: `supabaseServer`, `AnketKarti` bileşeni
- Produces: `/` rotası — aktif ve tamamlanan anket listesi

- [ ] **Step 1: `src/routes/+page.server.ts` yaz**

```typescript
import type { PageServerLoad } from './$types';
import { supabaseServer } from '$lib/supabase';

export const load: PageServerLoad = async () => {
  const { data: anketler } = await supabaseServer
    .from('anketler')
    .select('id, slug, soru, secenek_a, secenek_b, aktif, created_at')
    .order('created_at', { ascending: false });

  if (!anketler) return { aktif: [], kapali: [] };

  // Her anket için oy sayılarını çek
  const anketlerOylu = await Promise.all(
    anketler.map(async (anket) => {
      const [{ count: oy_a }, { count: oy_b }] = await Promise.all([
        supabaseServer.from('oylar').select('*', { count: 'exact', head: true }).eq('anket_id', anket.id).eq('secim', 'A'),
        supabaseServer.from('oylar').select('*', { count: 'exact', head: true }).eq('anket_id', anket.id).eq('secim', 'B')
      ]);
      return { ...anket, oy_a: oy_a ?? 0, oy_b: oy_b ?? 0 };
    })
  );

  return {
    aktif: anketlerOylu.filter((a) => a.aktif),
    kapali: anketlerOylu.filter((a) => !a.aktif)
  };
};
```

- [ ] **Step 2: `src/routes/+page.svelte` yaz**

```svelte
<script lang="ts">
  import AnketKarti from '$lib/components/AnketKarti.svelte';
  export let data;
</script>

<svelte:head>
  <title>Halk Oylaması</title>
</svelte:head>

<div class="max-w-2xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold mb-6">Güncel Anketler</h1>

  {#if data.aktif.length === 0}
    <p class="text-gray-500">Şu an aktif anket bulunmuyor.</p>
  {:else}
    <div class="space-y-3 mb-10">
      {#each data.aktif as anket (anket.id)}
        <AnketKarti {anket} />
      {/each}
    </div>
  {/if}

  {#if data.kapali.length > 0}
    <h2 class="text-lg font-semibold text-gray-600 mb-3">Tamamlanan Anketler</h2>
    <div class="space-y-3">
      {#each data.kapali as anket (anket.id)}
        <AnketKarti {anket} />
      {/each}
    </div>
  {/if}
</div>
```

- [ ] **Step 3: Ana sayfanın çalıştığını doğrula**

```bash
npm run dev
```

`http://localhost:5173` adresinde test verisindeki 2 anketin listelendiğini görmelisin.

- [ ] **Step 4: Commit**

```bash
git add src/routes/+page.svelte src/routes/+page.server.ts
git commit -m "feat: ana sayfa anket listesi"
```

---

### Task 8: Anket Detay Sayfası

**Files:**
- Create: `src/routes/anket/[slug]/+page.server.ts`
- Create: `src/routes/anket/[slug]/+page.svelte`

**Interfaces:**
- Consumes: `supabaseServer`, `SonucBar`, `OyFormu` bileşenleri
- Produces: `/anket/[slug]` rotası — soru + sonuçlar + oy formu

- [ ] **Step 1: `src/routes/anket/[slug]/+page.server.ts` yaz**

```typescript
import { error } from '@sveltejs/kit';
import type { PageServerLoad } from './$types';
import { supabaseServer } from '$lib/supabase';

export const load: PageServerLoad = async ({ params }) => {
  const { data: anket, error: dbError } = await supabaseServer
    .from('anketler')
    .select('id, slug, soru, secenek_a, secenek_b, aktif')
    .eq('slug', params.slug)
    .single();

  if (dbError || !anket) {
    throw error(404, 'Anket bulunamadı');
  }

  const [{ count: oy_a }, { count: oy_b }] = await Promise.all([
    supabaseServer.from('oylar').select('*', { count: 'exact', head: true }).eq('anket_id', anket.id).eq('secim', 'A'),
    supabaseServer.from('oylar').select('*', { count: 'exact', head: true }).eq('anket_id', anket.id).eq('secim', 'B')
  ]);

  return { anket, oy_a: oy_a ?? 0, oy_b: oy_b ?? 0 };
};
```

- [ ] **Step 2: `src/routes/anket/[slug]/+page.svelte` yaz**

```svelte
<script lang="ts">
  import SonucBar from '$lib/components/SonucBar.svelte';
  import OyFormu from '$lib/components/OyFormu.svelte';

  export let data;

  let oyA = data.oy_a;
  let oyB = data.oy_b;

  $: toplam = oyA + oyB;

  function oyKaydedildi(event: CustomEvent<{ oylar: { a: number; b: number } }>) {
    oyA = event.detail.oylar.a;
    oyB = event.detail.oylar.b;
  }
</script>

<svelte:head>
  <title>{data.anket.soru} — Halk Oylaması</title>
</svelte:head>

<div class="max-w-xl mx-auto px-4 py-8">
  <a href="/" class="text-sm text-blue-600 mb-6 inline-block">← Tüm anketler</a>

  <div class="bg-white rounded-xl border border-gray-200 p-6 mb-6">
    <div class="flex justify-between items-start mb-6">
      <h1 class="text-xl font-bold text-gray-900 leading-snug">{data.anket.soru}</h1>
      <span class="text-xs px-2 py-1 rounded-full ml-2 shrink-0 {data.anket.aktif ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'}">
        {data.anket.aktif ? 'Aktif' : 'Kapalı'}
      </span>
    </div>

    <div class="mb-4">
      <SonucBar secenek={data.anket.secenek_a} oy={oyA} {toplamOy} />
      <SonucBar secenek={data.anket.secenek_b} oy={oyB} {toplamOy} />
      <p class="text-xs text-gray-400 mt-2 text-right">{toplam} toplam oy</p>
    </div>
  </div>

  {#if data.anket.aktif}
    <div class="bg-white rounded-xl border border-gray-200 p-6">
      <h2 class="font-semibold mb-4">Oy Kullan</h2>
      <OyFormu
        anketId={data.anket.id}
        secenekA={data.anket.secenek_a}
        secenekB={data.anket.secenek_b}
        on:oyKaydedildi={oyKaydedildi}
      />
    </div>
  {:else}
    <p class="text-center text-gray-500 text-sm">Bu anket kapatılmıştır.</p>
  {/if}
</div>
```

> **Not:** `toplamOy` reactive variable için `$: toplamOy = oyA + oyB;` satırını script bloğuna ekle (`toplam` yerine; her iki ismi de tutarlı kullanmaya dikkat et — `SonucBar` `toplamOy` prop bekler).

- [ ] **Step 3: Anket sayfasının çalıştığını doğrula**

`http://localhost:5173/anket/turkiye-ab` adresini aç. Şunları görmelisin:
- Soru başlığı
- Sonuç bar chart (başlangıçta 0%)
- Oy kullanma formu (3 adım: seçim → e-posta → kod)

- [ ] **Step 4: Uçtan uca testi manuel yap**

1. Bir seçenek seç
2. Gerçek e-posta adresin gir
3. Gelen kodu gir
4. "Oyunuz kaydedildi!" mesajını ve güncellenen bar'ı doğrula
5. Aynı e-posta ile tekrar oy vermeye çalış → "zaten oy kullandınız" hatası görmeli

- [ ] **Step 5: Commit**

```bash
git add src/routes/anket/
git commit -m "feat: anket detay sayfası (sonuçlar + oy formu)"
```

---

### Task 9: Son Kontroller ve Deploy Hazırlığı

**Files:**
- Modify: `svelte.config.js` — adapter ekle
- Create: `vercel.json` (opsiyonel)

**Interfaces:**
- Produces: Deploy edilebilir uygulama

- [ ] **Step 1: Vercel adapter ekle**

```bash
npm install -D @sveltejs/adapter-vercel
```

`svelte.config.js`:

```javascript
import adapter from '@sveltejs/adapter-vercel';
import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';

export default {
  preprocess: vitePreprocess(),
  kit: { adapter: adapter() }
};
```

- [ ] **Step 2: Tüm testleri çalıştır**

```bash
npm run test
```

Beklenen: tüm testler PASS

- [ ] **Step 3: Build kontrolü**

```bash
npm run build
```

Beklenen: hata yok

- [ ] **Step 4: `.env` değişkenlerini Vercel'e ekle**

Vercel dashboard → Settings → Environment Variables:
- `PUBLIC_SUPABASE_URL`
- `PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_KEY`

- [ ] **Step 5: Commit**

```bash
git add svelte.config.js package.json package-lock.json
git commit -m "chore: vercel adapter ve deploy hazırlığı"
```
